[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# django
# ==========================================
django = "~=3.2.13"
django-allauth = "~=0.50"  # auth flows
# scrapping
# ==========================================
beautifulsoup4 = "~=4.11.1"
requests = "~=2.27.1"  # make http request
# utils
# ==========================================
pillow = "~=9.1.1"  # image processing

[dev-packages]
# linting
# ==========================================
black = "==22.3.0"
# utils
# ==========================================
django-extensions = "~=3.1.5"

[requires]
python_version = "3.10"
